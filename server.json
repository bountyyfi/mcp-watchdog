{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-07-09/server.schema.json",
  "name": "io.github.bountyyfi/mcp-watchdog",
  "description": "MCP security proxy that detects and blocks all known MCP attack classes. Catches prompt injection, rug pulls, tool poisoning, tool shadowing, SSRF, command injection, SQL injection, reverse shells, token leakage, OAuth hijacking, and 40+ other attack types. Zero config - just wrap your existing MCP server command.",
  "version": "0.1.1",
  "packages": [
    {
      "registry_type": "pypi",
      "identifier": "mcp-watchdog",
      "version": "0.1.1"
    }
  ]
}
