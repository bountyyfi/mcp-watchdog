{
  "_comment": "Windsurf MCP configuration with mcp-watchdog proxy.",
  "mcpServers": {
    "mcp-watchdog": {
      "command": "mcp-watchdog",
      "args": ["--verbose"],
      "env": {}
    }
  }
}
