{
  "_comment": "Claude Desktop: wrap your MCP server with mcp-watchdog. Replace the original server entry with this pattern — the original command goes after '--'.",
  "mcpServers": {
    "filesystem-watchdog": {
      "command": "mcp-watchdog",
      "args": ["--verbose", "--", "npx", "-y", "@modelcontextprotocol/server-filesystem", "/Users/you/projects"],
      "env": {}
    },
    "sqlite-watchdog": {
      "command": "mcp-watchdog",
      "args": ["--verbose", "--", "npx", "-y", "@modelcontextprotocol/server-sqlite", "/path/to/db.sqlite"],
      "env": {}
    }
  }
}
