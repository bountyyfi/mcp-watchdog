{
  "_comment": "Cursor: wrap your MCP server with mcp-watchdog. Replace the original server entry with this pattern — the original command goes after '--'.",
  "mcpServers": {
    "filesystem-watchdog": {
      "command": "mcp-watchdog",
      "args": ["--verbose", "--", "npx", "-y", "@modelcontextprotocol/server-filesystem", "/Users/you/projects"],
      "env": {}
    }
  }
}
